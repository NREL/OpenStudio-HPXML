version: 2
jobs:
  build:
    docker:
      - image: nrel/openstudio:2.7.0
    steps:
      - checkout
      - run: 
          name: Run tests
          command: |
            ruby tests/hpxml_translator_test.rb
      - store_artifacts:
          path: tests/results
          destination: results
